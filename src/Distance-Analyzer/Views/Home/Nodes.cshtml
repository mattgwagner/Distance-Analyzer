@model IEnumerable<Distance_Analyzer.Models.Node>

@Html.ActionLink("Add Node", "Scrub")

<table class="table">

    @foreach (var node in Model)
    {
        <tr>
            <td>@Html.ActionLink(node.Id.ToString(), "Node", new { node.Id })</td>
            <td>@Html.DisplayFor(_ => node.Description)</td>
            <td>@Html.DisplayFor(_ => node.Address)</td>
            <td>@Html.DisplayFor(_ => node.Is_Super_Node)</td>
        </tr>
    }
</table>